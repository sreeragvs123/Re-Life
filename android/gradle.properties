org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
android.ndkVersion=23.1.7779620

# --- Java 21 upgrade helper ----------------------------------------------
# To build this project with a specific JDK (recommended for upgrades),
# install a JDK 21 distribution and then either set your system JAVA_HOME
# or point Gradle at the JDK by uncommenting and editing the line below.
# Example (Windows):
# org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-21.0.0
#
# Notes:
# - If you prefer to use the system JDK, set JAVA_HOME in Environment Variables
#   and ensure `java -version` prints the JDK 21 runtime.
# - After installing JDK 21, update the Gradle wrapper (see UPGRADE_JAVA21.md)
#   to a Gradle version that supports Java 21 (Gradle 8.6+ recommended).
# - Keep this file under version control; only change `org.gradle.java.home`
#   if you want this project to use a non-system JDK.
# -------------------------------------------------------------------------

